
doctype
html
    head
        title= "Stream to Wav"
    body
        div()
            h1 #{pageData.songTitle}
        form(action="/upload", method="post", enctype="multipart/form-data")
            input(type="file", name="wav")
            input(type="submit", value="Send")